/**
 * Month orchestration: startMonth / applyAction / endMonth (placeholder — Phase 3).
 * GDD resolution order must be preserved when implemented.
 */

export function startMonthPlaceholder(): void {
  // Phase 3
}

export function endMonthPlaceholder(): void {
  // Phase 3
}
